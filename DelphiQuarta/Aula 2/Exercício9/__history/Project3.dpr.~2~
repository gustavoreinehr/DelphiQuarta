program Project3;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

var
idades: array of integer;
i: integer;
media: Double;

begin
  try

Writeln('Escreva a idade do aluno (0 se for encerrar)');
repeat
  readln(idades);
until ;






    { TODO -oUser -cConsole Main : Insert code here }
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
